# 报告素材

## 项目定位

本任务围绕信息安全课程大作业中的数字水印方向展开，重点关注鲁棒图像水印、版权保护、内容溯源、篡改/裁剪/拼接后的水印检测与恢复。

当前优先考虑的技术路线是：

> 以 WAM（Watermark Anything with Localized Messages）作为主论文候选，以 DWSF（Practical Deep Dispersed Watermarking with Synchronization and Fusion）的分散嵌入、同步定位、消息融合思想作为主要改进来源。

## 当前阶段结论

WAM 目前比 DWSF 更适合作为主论文候选，原因是 WAM 官方代码和预训练权重更明确，能在本地先跑通推理和攻击评测；DWSF 思想很适合改进 WAM，但 DWSF 本体训练依赖 A100 且缺少现成预训练权重，在 4060 上直接复现主结果风险较高。

当前主论文候选优先级：

1. WAM / Watermark Anything with Localized Messages
2. EditGuard
3. TrustMark
4. CRMark
5. Robust-Wide / VINE

当前不建议作为主论文的对象：

- PIMoG / RoPaSS：物理拍屏依赖强，复现实验不稳定。
- DWSF：适合作改进思想来源，但直接作主论文复现风险高。
- RAIMark / Ultra-high Resolution：方向贴合但代码和训练资源风险较大。
- CIN / FIN / MuST / SepMark：可作为参考或基线思想，但直接大改风险偏高。

## WAM 为什么适合作主论文

WAM 的核心任务是局部化图像水印。它可以在图像局部区域嵌入消息，并在图像经历裁剪、拼接、局部编辑或 inpainting 后，检测哪些区域含有水印并恢复对应消息。

它适合作主论文的理由：

- 方向属于数字水印、版权保护、内容溯源，符合信息安全课程范围。
- 官方代码和预训练权重公开，先跑通推理和实验的概率较高。
- 支持局部水印和多消息，与 DWSF 的多块分散嵌入、消息融合思想天然相近。
- 实验容易设计：可比较单区域嵌入、多区域嵌入、多区域多数投票、多区域置信融合等方案。

主要风险：

- 如果只调用 WAM 并加一个简单投票，会显得创新不足。
- 需要把 DWSF 式分散冗余嵌入和融合恢复设计成完整实验，而不是简单后处理。
- WAM 本身不是专门解决几何同步校正的论文，因此不要硬吹完整几何鲁棒，应重点放在裁剪、拼接、局部破坏、多区域融合恢复。

## DWSF 可借鉴思想

DWSF 的主要可迁移思想：

- 分散嵌入：在多个区域重复嵌入同一消息，提高裁剪和局部破坏后的存活率。
- 同步定位：先定位水印区域，再进行解码，缓解攻击后水印块位置变化的问题。
- 消息融合：多个区域分别解码后，根据相似度、置信度或一致性融合出最终消息。

拟用于 WAM 的改进方向：

- 在同一张图中选择多个局部区域嵌入同一消息。
- 攻击后利用 WAM 的 detection mask 找到水印候选区域。
- 对多个区域输出的 bit 结果进行 majority vote、confidence fusion 或 DWSF-style similarity fusion。
- 针对 crop、splicing、local occlusion、inpainting、JPEG、resize 等攻击评估 bit accuracy 和 detection quality。

## 初步实验设计

待验证实验组：

1. 原始 WAM 单区域嵌入与解码。
2. 多区域重复嵌入，但不做融合。
3. 多区域重复嵌入，使用 majority vote。
4. 多区域重复嵌入，使用置信度加权融合。
5. 多区域重复嵌入，使用 DWSF-style similarity fusion。

待评估攻击：

- Random crop
- Center crop
- Local occlusion
- Splicing
- Inpainting
- JPEG compression
- Resize
- Resize + JPEG

待记录指标：

- Bit accuracy
- Message recovery rate
- Detection mask quality
- Watermarked image PSNR / SSIM / LPIPS
- 不同攻击强度下的鲁棒性曲线

## 素材同步规则

之后每完成一部分工作，同步记录到本文件：

- 论文阅读结论：记录论文做什么、能借鉴什么、硬伤是什么。
- 代码复现进度：记录环境、命令、成功/失败、错误原因。
- 实验结果：记录设置、输入、输出、指标、图表路径。
- 方案决策：记录为什么保留/淘汰某条路线。
- 报告可用文字：将能直接放入报告的段落整理到对应小节。

## 目录说明

- `实验记录/`：保存每次复现实验、攻击测试、参数设置和结果说明。
- `结果输出/`：保存模型输出图、攻击图、指标表、可视化结果。
- `报告素材附件/`：保存后续报告可直接引用的图片、表格、流程图。
- `脚本草稿/`：保存临时实验脚本或后续可整理进项目代码的脚本。

## 已有资料索引

- 海选论文目录：`C:\Users\86155\Desktop\信息安全\大作业\参考论文\DWSF相近论文海选`
- 海选清单：`C:\Users\86155\Desktop\信息安全\大作业\参考论文\DWSF相近论文海选\下载清单.md`
- 二轮筛选：`C:\Users\86155\Desktop\信息安全\大作业\参考论文\DWSF相近论文海选\二轮筛选.md`
- 原始鲁棒水印资料：`C:\Users\86155\Desktop\信息安全\大作业\参考论文\鲁棒图像水印资料`

## 进度记录

### 2026-07-06

- 创建任务目录：`C:\Users\86155\Desktop\信息安全\大作业\WAM-DWSF鲁棒水印任务`
- 创建报告素材文件：`报告素材.md`
- 当前路线暂定为：WAM 主论文候选 + DWSF 思想改进。
- 下一步建议：优先实际跑 WAM 官方代码和预训练模型，确认 4060 上推理、嵌入、提取、攻击评测能否跑通。
- 夜间长任务开始：优先跑通 WAM 全流程；若 WAM 复现遇到硬阻塞，自动切换 TrustMark / EditGuard 等备选主论文项目继续跑通。
- 题目硬要求记录：课程报告需围绕 CCF-B 以上相关论文，完成复现或改进；报告需包含背景动机、方案原理、实验验证与结果分析、代码运行截图和总结。数字水印方向与课程中的数据安全、完整性保护、认证/溯源和攻击防御相关。
- WAM 官方代码真实位置：`C:\Users\86155\Desktop\信息安全\大作业\参考论文\鲁棒图像水印资料\code\Watermark-Anything`
- 为任务目录添加 `.gitignore`，避免把模型权重、数据集和大体积原始输出提交进 git。
- 创建长任务执行清单：`长任务执行清单.md`。之后每完成一个阶段都回看该清单，继续执行下一项。
- 环境策略修正：优先使用现有 `bamboo` 环境，不优先新建环境重装 PyTorch。
- 已确认 `bamboo` 环境可用：Python 3.10，torch/torchvision 已安装，CUDA 可用，设备为 NVIDIA GeForce RTX 4060 Laptop GPU。
- 已在 `bamboo` 环境中补装 WAM 所需轻量依赖：omegaconf、einops、pycocotools、timm、lpips。
- 修正长任务阶段 8：后续若已完成基础复现和若干创新，不转去写 README、报告大纲、空泛图表或汇总表；也不单独扩展攻击/图片数量。持续任务只保留“读其他论文、找有明确论文来源的可实现创新、优化主论文并实验验证”。
- 下载 WAM 官方 MIT 权重 `wam_mit.pth` 成功，大小 377,825,938 bytes，SHA256 为 `90ef232384e023bd63245eb0c131abd69d2afc7b8f17a71ccedceb542bf009e2`。
- WAM 官方推理流程已跑通。使用官方示例图 `alpaca.jpg`、`ducks.jpg`、`gauguin_256.jpg`，完成单水印和多水印复现。
- 单水印实验：3 张图 bit accuracy 均为 1.000000，PSNR 分别为 38.8986、43.1886、42.0654。
- 多水印实验：3 张图均检测到 2 条消息，最佳 bit accuracy 均为 1.000000，PSNR 分别为 42.3100、46.1306、45.8596。
- 复现记录已写入：`实验记录\WAM官方复现.md`。
- 指标文件：`结果输出\wam_official_repro\wam_official_repro_metrics.csv`。
- 可视化输出：`结果输出\wam_official_repro\visuals`。
- 当前结论：WAM 在本机现有环境和 RTX 4060 Laptop GPU 上具备较好的复现基础，可以继续作为主论文候选推进。
- 完成 WAM 攻击评测基线，脚本为 `脚本草稿\wam_attack_eval.py`。
- 攻击评测覆盖：JPEG Q=95/85/75/65/50/30，resize 0.75/0.5/0.25，center crop 0.9/0.75/0.5，random crop 0.9/0.75/0.5，local occlusion 5%/10%/20%，partial removal 5%/10%/20%。
- 评测结果文件：`结果输出\wam_attack_eval\wam_attack_eval_metrics.csv` 和 `结果输出\wam_attack_eval\wam_attack_eval_summary.csv`。
- 攻击基线主要发现：WAM 对无攻击、轻中度 resize/crop、局部遮挡和 partial removal 表现很稳；主要弱点集中在强 JPEG、极端 resize 和强中心裁剪。
- 关键弱点数据：`jpeg_q30` 平均 bit accuracy 0.925000，最低 0.781250；`resize_0.25` 平均 0.962500，最低 0.812500；`center_crop_0.5` 平均 0.950000，最低 0.906250。
- 攻击评测记录已写入：`实验记录\WAM攻击评测基线.md`。
- 后续创新方向已经更明确：用 DWSF 式多区域冗余和融合改善裁剪/极端缩放表现，用 MBRS 式 JPEG 思想改善强 JPEG 表现，用 TrustMark 式强度权衡观察质量-鲁棒性边界。
- 完成 DWSF 式多区域冗余融合 v1，脚本为 `脚本草稿\wam_dwsf_redundant_eval.py`。
- v1 方案：baseline 为单个 50% 区域；DWSF-style 方案为 5 个 10% 区域重复嵌入同一消息，并尝试全局平均、组件多数投票、组件置信加权融合。
- v1 结果不是稳定正向：多数简单攻击两者都接近 1.0；`random_crop_0.5` 最低 bit accuracy 从 0.9375 提升到 0.96875，但 JPEG Q=50/Q=65 和强中心裁剪上出现退化。
- v1 结论：多区域冗余不能无条件提升所有攻击，尤其不能指望它直接改善 JPEG；它更适合局部大块破坏、区域丢失、裁剪类场景。
- v1 记录已写入：`实验记录\DWSF式多区域冗余融合_v1.md`。
- 下一步：做 DWSF v2，使用规则化空间分散布局，并构造更强的局部破坏/局部移除/角落裁剪攻击，让实验更贴近 DWSF 的适用场景。
- 完成 DWSF 式空间分散冗余 v2，脚本为 `脚本草稿\wam_dwsf_spatial_v2.py`。
- v2 方案：baseline 为单个中心 50% 区域；DWSF-style 方案为四角 + 中心共 5 个 10% 区域，总嵌入面积约 50%。
- v2 正向结果：对中心大块破坏有效，`remove_center_40` 从 mean/min 0.975000/0.875000 提升到 1.000000/1.000000，`black_center_40` 从 0.981250/0.906250 提升到 1.000000/1.000000。
- v2 负向结果：对 50% 裁剪退化，`crop_bottom_right_50` 从 0.975000/0.937500 降到 0.931250/0.750000，说明简单空间分散不足以解决裁剪后的尺度/位置同步问题。
- v2 结论：空间分散冗余可以作为局部破坏场景的有效改进，但不能直接作为最终创新方案；后续应引入 MBRS 式多预处理/多尺度解码或 TrustMark 式强度-质量权衡。
- v2 记录已写入：`实验记录\DWSF式空间分散冗余_v2.md`。
- 完成 MBRS 式多分支 JPEG 解码 v1，脚本为 `脚本草稿\wam_mbrs_multibranch_decode.py`。
- v1 论文来源：MBRS 的 real JPEG / simulated JPEG / identity 混合思想，以及 DWSF 的候选消息相似性选择思想。
- v1 正向结果：`jpeg_q20` 从 mean/min 0.850000/0.500000 提升到 0.868750/0.593750；`resize_0.75_jpeg_q50` 的 confidence select 从 0.962500/0.812500 提升到 0.968750/0.843750。
- v1 负向结果：`jpeg_q65`、`jpeg_q50` 等场景的 oracle 分支有潜力，但 confidence/similarity selector 未能稳定选中；`jpeg_q30` 和 `resize_0.25_jpeg_q50` 基本无改善。
- v1 结论：多分支真实/模拟 JPEG 解码可作为辅助增强和分析实验，但不够强，不建议单独作为最终创新主线。下一步优先做 TrustMark 式强度-质量权衡。
- v1 记录已写入：`实验记录\MBRS式多分支JPEG解码_v1.md`。
- 完成 TrustMark 式强度-质量权衡 v1，脚本为 `脚本草稿\wam_trustmark_strength_sweep.py`。
- v1 论文来源：TrustMark 的 residual scale factor / 强度-质量权衡思想，迁移到 WAM 的 `scaling_w` 参数。
- v1 关键结果：`scaling_w=1.5` 时 mean clean PSNR 43.6083、selected attack mean accuracy 0.931250；`scaling_w=2.0` 时 PSNR 41.1181、attack mean 0.948958；`scaling_w=2.5` 时 PSNR 39.1887、attack mean 0.967708；`scaling_w=4.0` 时 attack mean 0.984375 但 PSNR 仅 35.1348。
- v1 结论：强度扫描是成立的主改进轴。低强度虽然画质高但恢复不稳；2.0 是官方默认附近的稳健折中；2.5/3.0 适合鲁棒优先场景；4.0 不建议作为默认，因为画质代价大。
- 下一步：把 TrustMark 式强度权衡和 DWSF 式空间分散结合，比较单区域/多区域与不同强度组合。
- v1 记录已写入：`实验记录\TrustMark式强度质量权衡_v1.md`。
- 完成 DWSF 与 TrustMark 强度组合 v1，脚本为 `脚本草稿\wam_combined_dwsf_strength.py`。
- 组合实验核心结论：同一强度下，DWSF 五区域方案的 selected attack mean accuracy 均高于单中心方案，且 mean PSNR 略高。
- 最关键对比：`DWSF + scaling_w=2.5` 的 mean PSNR 为 39.7711、selected attack mean accuracy 为 0.964063；`single + scaling_w=3.0` 的 mean PSNR 为 37.5542、attack mean 为 0.963281。前者鲁棒性略高且 PSNR 高约 2.2 dB。
- 必须诚实写的限制：DWSF 组合对 `crop_bottom_right_50` 不如单中心高强度方案，说明空间分散还没有解决角落裁剪后的同步/尺度问题。
- 当前最适合作为最终主创新版本：WAM 主论文复现 + DWSF 式五区域空间分散 + TrustMark 式强度-质量权衡。
- v1 记录已写入：`实验记录\DWSF与TrustMark强度组合_v1.md`。
- 完成 DWSF 式 bbox 同步解码 v1，脚本为 `脚本草稿\wam_dwsf_bbox_sync_decode.py`。
- v1 论文来源：DWSF 的 mask 定位 + minimal bounding rectangle + synchronized blocks 思想，以及 EditGuard/OmniGuard 的 mask 辅助局部定位思想。
- v1 正向结果：对单中心区域收益明显一些，例如 `single + 2.5 + jpeg_q20` 从 0.712500/0.000000 提升到 0.812500/0.500000；对 DWSF 五区域只有小幅收益，例如 `DWSF + 3.0 + crop_bottom_right_50` 从 0.968750 提升到 0.975000。
- v1 限制：轻量 bbox 同步没有根本解决 DWSF 角落裁剪退化，实际 selector 多数场景只能持平或小幅提升；真正 DWSF 同步效果可能需要训练专门 segmentation/synchronization 模块。
- v1 记录已写入：`实验记录\DWSF式bbox同步解码_v1.md`。
- 完成 ECC 消息冗余编码 v1，脚本为 `脚本草稿\wam_payload_ecc_eval.py`。
- v1 论文来源：TrustMark 提到 error correcting code 可改善非完美 bit accuracy，MBRS 的 message processor 强调扩展消息并实现冗余。
- v1 正向结果：在 `DWSF + scaling_w=2.5` 下，`crop_center_50` 的 10-bit payload success 从 uncoded10 的 0.400000 提升到 rep3_10 的 1.000000；`resize_0.25_jpeg_q50` 从 0.800000 提升到 1.000000。
- v1 限制：对 `jpeg_q20` 不稳定，rep3_10 在 2.5 档 payload accuracy 从 0.840000 略降到 0.820000；说明强 JPEG 错误可能不是独立随机错误。该方案有容量代价，应写成可选 ECC 模式。
- v1 记录已写入：`实验记录\ECC消息冗余编码_v1.md`。
- 完成 DWSF 面积比例扫描 v1，脚本为 `脚本草稿\wam_dwsf_area_sweep.py`。
- v1 论文来源：DWSF 论文中关于分散嵌入总面积比例 `Q` 与块数上限的消融；原论文说明更大的 Q 往往提高 bit accuracy 但降低 PSNR，默认选择 Q=25%。
- v1 关键结果：`Q=30%, 5 blocks, scaling_w=2.5` 的 mean PSNR 为 42.0691、selected attack mean accuracy 为 0.957812、worst selected attack 为 0.887500；同一实验中的 `single_center_50pct` 为 PSNR 39.1467、attack mean 0.948438、worst 0.737500。
- v1 鲁棒优先结果：`Q=50%, 5 blocks` 的 attack mean 最高，为 0.968750，PSNR 39.7612；但从质量-鲁棒折中看，`Q=30%, 5 blocks` 更适合作为默认改进方案。
- v1 限制：DWSF 面积扫描不是全攻击胜利；`crop_top_left_50` 中单中心方案平均 1.000000，而 `Q=30%, 5 blocks` 为 0.937500。DWSF 的优势主要体现在强 JPEG 最差情况和 `resize_0.25_jpeg_q50` 上。
- v1 记录已写入：`实验记录\DWSF面积比例扫描_v1.md`。
- 完成自适应区域选择 v1，脚本为 `脚本草稿\wam_adaptive_region_select.py`。
- v1 论文来源：DWSF 的分散块选择，OmniGuard 的 content-aware localized watermark texture，WAM 的 JND/对比度掩蔽思想，以及 TrustMark 的质量-鲁棒权衡。
- v1 结论偏负面：`hybrid_texture_residual` 的 selected attack mean 为 0.948438，略高于固定布局 0.945312，但 PSNR 从 42.0139 降到 41.0139，worst selected attack 从 0.906250 降到 0.881250；不值得替代固定 `Q=30%, 5 blocks`。
- v1 额外发现：`low_residual` 能把 PSNR 提到 43.6530，但 selected attack mean 只有 0.890625，说明只按低残差选块会牺牲解码稳定性，尤其强 JPEG 不稳。
- v1 处理：不作为最终主创新，只作为负结果和排除分支记录。
- v1 记录已写入：`实验记录\自适应区域选择_v1.md`。
- 完成 ECC 编码变体 v1，脚本为 `脚本草稿\wam_payload_ecc_variants.py`。
- v1 论文来源：RoSteALS 的 ECC 指标、MBRS 的 message processor / message redundancy 思想，以及 TrustMark 对 provenance payload 的需求。
- v1 关键结果：在 `DWSF Q=30%, 5 blocks, scaling_w=2.5` 下，`uncoded10`、`rep3_adjacent10`、`rep3_interleaved10`、`hamming74_10`、`hamming74_interleaved10` 的 mean selected payload success 都是 0.760000，没有产生整体胜者。
- v1 正向点：`rep3_adjacent10`、`hamming74_10`、`hamming74_interleaved10` 把 worst selected payload success 从 uncoded/interleaved-rep 的 0.400000 提升到 0.600000。
- v1 限制：`rep3_interleaved10` 对角落裁剪最好但强 JPEG 退化；Hamming 只能稳住最差情况，不能根本解决强 JPEG。该模块适合写成可选 ECC 模式，不适合作为最终主创新。
- v1 记录已写入：`实验记录\ECC编码变体_v1.md`。
- 完成主动篡改定位 v1，脚本为 `脚本草稿\wam_tamper_localization_eval.py`。
- v1 论文来源：EditGuard 的 proactive tamper localization、OmniGuard 的主动水印 + passive extraction 思想，以及 DWSF 的稀疏分散覆盖。
- v1 关键结果：`DWSF Q=30%, 5 blocks` 在 prob_drop localizer 下 covered F1 为 0.985547、covered IoU 为 0.971861，bit accuracy 为 1.000000；但 tamper coverage 只有 0.271703，因此 global F1 只有 0.420431。
- v1 安全优先结果：`DWSF Q=50%, 5 blocks` 的 covered F1 为 0.973085、global F1 为 0.635645、tamper coverage 为 0.486693，bit accuracy 仍为 1.000000。
- v1 判断：这是可保留的附加创新分支，能把版权追踪扩展到局部完整性/篡改定位；但不能夸大，因为它只能定位水印覆盖到的区域。
- v1 记录已写入：`实验记录\主动篡改定位_v1.md`。
- 完成二次水印空间分区 v1，脚本为 `脚本草稿\wam_rewatermarking_eval.py`。
- v1 论文来源：TrustMark 的 re-watermarking / provenance update 场景，WAM 的 multiple localized watermarks 能力，以及 DWSF 的空间分区思想。
- v1 关键结果：`overlap_replace_same_region` 会保留新消息 B，但旧消息 A 平均 accuracy 只有 0.522917，both success 为 0；`disjoint_append_side_regions` 的 A/B 平均 accuracy 分别为 0.966667/0.952083，both success 为 0.800000。
- v1 代价：空间分区追加的 PSNR 为 39.0893，低于同区域覆盖的 42.0668，因为总嵌入面积更大。
- v1 判断：这是可保留的 provenance update / 多阶段授权分支，不适合作为默认低扰动方案，但可以作为安全优先模式。
- v1 记录已写入：`实验记录\二次水印空间分区_v1.md`。
- 完成实用编辑与平台变换下的模式选择 v1，脚本为 `脚本草稿\wam_practical_transform_modes.py`。
- v1 论文来源：Robust-Wide 对 instruction-driven editing 和真实编辑链路的关注，FlexMark 对容量/鲁棒/不可感知权衡与 WebP/JPEG 平台转换的关注，以及 TrustMark/DWSF 的强度和面积比例权衡。
- v1 关键结果：`dwsf_default_q30_s2.5` 的 PSNR 为 42.0144、selected attack mean 为 0.977885、worst selected attack 为 0.912500；`dwsf_robust_q50_s2.5` 的 PSNR 为 39.7046、attack mean 为 0.988462、worst 为 0.943750；`dwsf_robust_strong_q50_s3.0` 的 PSNR 为 38.1298、attack mean 为 0.992788、worst 为 0.950000。
- v1 与 single 强嵌入对比：`single_center50_s3.0` 的 attack mean 同为 0.992788、worst 为 0.956250，但 PSNR 只有 37.5249；`dwsf_robust_strong_q50_s3.0` PSNR 高约 0.6 dB，同时保留 DWSF 空间分散对篡改定位和多阶段授权分支的兼容性。
- v1 判断：后续主方案不应只给一个配置，而应给三档模式：质量优先 `Q=30%, 5 blocks, scaling_w=2.5`；均衡鲁棒 `Q=50%, 5 blocks, scaling_w=2.5`；安全/平台鲁棒优先 `Q=50%, 5 blocks, scaling_w=3.0`。
- v1 记录已写入：`实验记录\实用编辑平台变换模式选择_v1.md`。
- 完成 MuST 式多源合成追踪 v1，脚本为 `脚本草稿\wam_must_composite_tracing.py`、`脚本草稿\wam_must_composite_tracing_ecc.py` 和 `脚本草稿\wam_must_codebook_match.py`。
- v1 论文来源：MuST 的 multi-source tracing + MER 重同步思想，结合 WAM localized messages、DWSF 覆盖策略，以及 MBRS/RoSteALS 的消息冗余/ECC 思想。
- v1 必要性发现：在无合成后二次压缩时，`must_mer_resize` 和 `oracle_box_raw` 对多源合成中的每个 source ID 都能达到 1.000000；`global_full_canvas` 基本失败，说明多源合成不能整图混合解码，必须先定位/裁剪/重同步。
- v1 模式选择发现：小素材被缩小、裁剪、再压缩时，DWSF 稀疏覆盖不适合直接做 source tracing；基础 32-bit 解码中 `full_material_s3.0 + must_mer_resize` 的 mean source accuracy 为 0.626953，高于 `dwsf_q50_s2.5 + must_mer_resize` 的 0.480469。
- v1 ECC 发现：rep4 短 ID 冗余能改善个别困难样例，但单靠 payload 完全恢复不够稳定；`rep4_interleaved_8bit + dwsf_q50_s3.0 + must_mer_resize` 的 payload success rate 为 0.312500。
- v1 codebook 发现：注册 source ID 最近码字匹配更符合溯源任务。整体上 `rep4_adjacent_8bit + full_material_s2.5 + must_mer_resize` 的 codebook strict match rate 为 0.531250；在 `two_sources_downsize` 场景中，`full_material_s2.5 + must_mer_resize` 面对 JPEG Q50、JPEG Q70、resize 0.5 + JPEG Q70 时 codebook strict match rate 均达到 1.000000。
- v1 限制：当前使用 oracle box 模拟 detector/MER，没有训练真实 multi-source detector；三源极端缩小 + 压缩仍不稳定。因此该分支适合作为多源版权溯源扩展，不宜写成主方案已完整解决 MuST 任务。
- v1 记录已写入：`实验记录\MuST式多源合成追踪_v1.md`。
